<{{typeCheck this.type}}
	{{#each this.props}}{{#each this}}{{{setMapCoords @key this}}}{{/each }}{{/each }}
	style={[{{#each this.className}}{{#unless @first}}styles.{{camelCase this}}{{#unless @last}}, {{/unless }}{{/unless }}{{/each }}]}>
</{{typeCheck this.type}}>